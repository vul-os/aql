# aql — working todo

Scratch list. The structured version is ROADMAP.md; this is the running one.

## direction (see ARCHITECTURE.md)
- Aql = one hub that owns everything physical. Seven device kinds: camera, lighting,
  robot, climate, energy, sensor, access. Access is the most complete and the only one
  that does NOT go through the device engine — it is still a parallel stack.
- Chat is an input surface across the hub, not the product.
- Shipped: the hub (open path, console, audit, SQLite), the controller agent
  (pinned-key pairing, fail-closed verification, offline LAN grants), signed commands,
  proto/'s conformance vectors, the e2e harnesses, the device engine + four drivers,
  the automations runtime, energy metering, and the console screens over all of it.

## naming, settled
- The hub is a **hub**. Never "the gateway" in prose — in the KOTVA family that word is
  the legacy-rail coordinator role, which is Ephor's job. The directory is `hub/`.
- Still spelled the old way, on purpose, as compat surface: `LINTEL_*` env vars (read
  behind `lookupEnv`, with a startup warning), `lintel.db`, `_lintel._tcp`,
  `lintel.offline-access` (an IndexedDB rename orphans rather than migrates), and
  proto/vectors/. Guarded by src/lib/__tests__/naming.test.ts.

## next up
- [ ] BLE radio validation on real hardware — framing and session are tested, the radio
      has never run. `-tags ble` builds a real GATT server on Linux (BlueZ) AND Windows
      (WinRT); only darwin and below get ErrUnsupported from
      `bleperiph/start_unsupported.go`, and that one is blocked upstream:
      tinygo.org/x/bluetooth v0.15.0 has no GATT server for darwin at all
      (gatts_other.go is a nine-line stub), so macOS is a BLE central only.
- [ ] GPIO relay validation on real hardware — the driver exists behind `-tags gpio` and
      has never driven a chip, a relay board or a gate. Fail-safe spec: normally-open,
      line drops on exit/panic. The uAPI layout IS now verified on linux/arm64, arm and
      386 at compile time (gpio_abi_static.go), so what remains is genuinely the radio
      and the relay board, not the struct definitions.
- LESSON: CI built the tagged backends but never TESTED them. `go test ./...` cannot see
      a file behind //go:build gpio, so three test files had never run in the pipeline.
      A tagged backend needs both a build step and a test step.
- [x] Present an offline grant over LAN from a browser. The controller now allows the
      ONE origin of the hub console it paired with (lanserver/cors.go), so an
      http-served console can complete the handshake. Desktop shell unchanged.
- [ ] Present over LAN from an HTTPS console — blocked by mixed content, not by CORS,
      and no header fixes it. Would need the controller to serve TLS on the LAN, which
      means a cert for a .local name. Open question, not a task yet.
- [ ] Present over BLE from a browser — genuinely impossible; needs native code in
      src-tauri (also the only way to resolve .local reliably). Tracked as the
      offline-grant-app-client claim.
- [ ] Bring access onto the internal device model, so it is one kind among seven rather
      than a parallel stack.
- [ ] A robot driver — the one kind of the seven with no path at all.

## blocked on something outside this repo
- [ ] Matter — needs a certified device and a stack.
- [ ] Modbus RTU (serial). TCP ships; a TCP-to-RTU bridge covers the common case.
- [ ] The camera pipeline: H.264 depacketization and an fMP4 writer. The ONVIF driver
      discovers cameras, probes streams with RTSP, and reads RTP headers — packet counts
      and sequence-number loss, so "flowing" and "intact" are separate answers — but has
      never decoded a frame. Depacketization is the line: a fake written from the same
      RFC would agree with the code under test, so it waits for a real camera.
      `docs/CAMERA-RETENTION.md` is the design that has to exist first — it is design
      only, and steps 3–5 in it (retention worker, `camera:view` permission, viewer) do
      NOT need hardware.
- [ ] Ephor chat-rail migration steps 1–7 (docs/EPHOR-CHAT-SEAM.md) — needs a kotva retag
      and a crate that does not exist yet.
- [ ] COORD-2 — filed to ephor/COORDINATION.md, awaiting a spec ruling. `LockIn` has two
      states and cannot express mode-dependent lock-in. Pinned by a test that fails when
      the answer changes.

## chat rail → Ephor (in progress)
- [x] COORD-3 examined. It is a DECISION already worked out in docs/EPHOR-CHAT-SEAM.md
      §1.5 (declare SelfHost::Backstop, refuse the ScarceReachabilityException the
      harness would grant), not a blocked question like COORD-2 — but the declaration
      lives in an Ephor crate that does not exist yet, so nothing to implement here.
      What WAS actionable: Aql's own §26.3 disclosure disagreed with §1.5's transport
      analysis for Telegram. Fixed.
- [ ] COORD-6: none of the four Gate variants is honest for this adapter (§1.6). Has a
      proposal (Gate::Authorization + a rustdoc disclosure in node mode, and propose
      Gate::AuthorizationDelegated upstream). Same blocker — needs the crate.
- [ ] the WhatsApp / Slack / Telegram adapters move OUT of Aql and into Ephor
      (github.com/vul-os/ephor). Target shape: Ephor terminates the rail and hands the hub
      an authorised command; the hub checks rules, signs, actuates. Swappable — run one or
      point at one.
- [ ] `hub/internal/channels/` is transitional until that lands. Don't document it as
      the supported path; don't rip it out either — it's what works today.
- [ ] `scripts/feature-claims.manifest.mjs` still has `whatsapp-channel`, `slack-channel`,
      `telegram-channel`, `slack-socket-mode` as shipped Aql claims. The code IS still
      there, so those entries are truthful today — revisit when the move lands rather
      than weakening them now.
- [x] Telegram long-polling (`getUpdates`) — shipped as an opt-in engine
      (`AQL_TELEGRAM_ENGINE=polling`). The poller had been complete and tested for a
      while with nothing constructing it; wiring is now covered by a reachability test.
- [ ] Discord — designed into the seam, no code yet. Same question: whose seam?
- [ ] extend the intent vocabulary past `open`/`close` so chat reaches the other kinds
- [ ] DMTAP channel stays a scaffold with a fail-closed transport until there is a real
      wire format. Do not present it as a channel.

## spec-derived checks
The technique that found the 2^53 rounding: derive expected behaviour from proto/*.md
and check the code against it, rather than checking the code against itself. The vectors
cannot catch what both implementations get wrong together.
- [x] pairing.md's "`repair` must be signed by the CURRENTLY pinned key". Honoured —
      Process reads the pinned key fresh, verifies, and only then swaps. But only the
      HAPPY path was tested. Added the attack: a repair signed by an unrelated key, and
      one self-signed by the key it installs. The second is the shape a well-meant fix
      for the documented lost-key limitation would take, and it is a complete takeover;
      simulating that change makes the controller answer result=ok.
- [x] commands.md §4's "remembered until exp + skew (after which step 3 rejects it
      anyway)". Honoured — Mark's horizon and Verify's expiry bound align exactly, so no
      replay window exists. It was untested, and it is a coupling across two files that
      neither side's tests can see.
- NOTE: pruning is LAZY. Seen() checks membership only; entries are dropped inside
      Mark(), which a replay never reaches. So a sweep that only replays proves nothing —
      my first version did exactly that and passed against a deliberately broken horizon.
      The test now accepts a fresh command at each step, which is what triggers the prune
      and is also what a controller in use actually does.
- [x] grants.md's 11-step verification ORDER ("first failure wins"). Honoured,
      including the one apparent deviation — the cnonce lookup precedes step 3 because
      with no pending entry there is no grant to verify, and the code says so. Untestable
      by vectors, which are single-fault by design: five multi-fault cases now assert the
      EARLIER step wins. Verified that moving lockdown after the signature check leaves
      the whole vector corpus AND verify.mjs green while the new test fails.
- [ ] Remaining normative claims worth the same treatment: grants.md's revocation
      window, events.md's ordering/at-least-once.

## fuzzing
- [x] No fuzz target existed anywhere. Four now, on the two parsers that read
      UNAUTHENTICATED bytes: mDNS parseResponse/decodeName (any host on the LAN, one
      UDP datagram) and the LAN grant HandleOpen/HandleProof (any host on the LAN, one
      POST at a gate). ~5M executions by hand found nothing; 30s each in CI as a
      tripwire. Seeds run free in the ordinary suite.
- [x] controller/internal/framing (BLE frames from anyone in radio range). Found a
      real gap: Push returns a COMPLETED message alongside frame_too_large when a valid
      frame precedes an oversized header in one write. Safe, because blesession checks
      the error first — but nothing tested that ordering, and the existing
      frame_too_large test could not reach the case. Reversing two statements in the
      session issues a challenge on a connection being dropped for protocol abuse.
- [x] controller/internal/jcs — fuzzed for PROPERTIES, not just panics: idempotent,
      deterministic across map iterations, value-preserving, output re-parses. "Does not
      panic" is far too weak for the code that decides which bytes a signature covers.
- [x] hub/internal/keys JCS (the signing side). Same properties.
- [x] proto/jcs-cases.json — the two JCS implementations cannot import each other
      (Go's internal/ rule, and e2e depends on neither by design), so their agreement is
      asserted against SHARED DATA. Expected values derived from RFC 8785 by hand, not
      captured from either side. Found a real defect: 2^53+1 was silently ROUNDED to
      2^53 rather than refused, because Float64() lost the precision before the range
      check ran — in BOTH implementations. The documented deviation promises a refusal.
- [ ] Parsers still unfuzzed: hub/internal/devices/modbus decode, the camera RTSP/RTP
      reader.
- NOTE: two of my own fuzz assertions were wrong before the code was. The framing one
      forbade messages alongside an error; the JCS one compared numbers by SPELLING, so
      canonicalising `0e00` to `0` — exactly correct — looked like value corruption. A
      property assertion is a claim about the spec and deserves the same scepticism as
      the code.

## race detector
- [x] Never run on this repo before. Run by hand across hub, controller and e2e, plus
      the gpio and ble tagged builds: CLEAN, no data races anywhere. Now a CI job so it
      stays that way.
- NOTE: hub/internal/httpapi takes ~18 min under -race because every register/login
      derives argon2id at 64 MiB. The first run FAILED at 608s — that was the default
      600s `go test` timeout, which reports exactly like a race. Timeouts are set
      explicitly in the job. The hashing parameters are deliberately not lowered for
      tests.

## relay safety
- [x] The MaxHold watchdog bounded ONE latch, not continuous energised time. Repeated
      `hold` commands re-latched after each expiry — measured 396ms continuously
      energised against a 150ms bound. Fixed with a cooldown equal to MaxHold. Found by
      investigating a "flaky" test instead of re-running it: the test sampled once at
      the end and landed in the brief de-energised gap about a third of the time.
- LESSON: a flaky test on a safety behaviour is the worst combination — it gets re-run
      until green and then trusted. Sample throughout and assert on the worst case.
- [x] Swept the relay tests for the same shape. Three slept a fixed time into a running
      pulse and then acted; the sleep began before the pulsing goroutine was scheduled,
      so the real margin was never what the test appeared to allow. They wait for the
      observed state now. Checked Pulse for the Hold defect too — MinPulse/MaxPulse are
      documented as bounding ONE pulse, and repeated pulses are the rate limiter's
      concern, so there is no unmet claim there.

## drift guards — audited for vacuous passes
The Go side too: bleperiph/platform_test.go's filename-constraint guard had the same
"look at every file, report offenders" shape and passed on zero files. Its sibling test
was worse — its comment named the assertion that matters (the no-backend build must
return exactly ErrUnsupported, because agent.go treats anything else as fatal and would
stop the controller) and the code only logged. Both fixed; BackendLinked is now a
compile-time constant per backend so a test can tell a stub build from a real one.
Every source-scanning test was checked by breaking its extraction and seeing whether it
noticed. naming.test.ts did NOT: four of its five guards passed with walk() returning
nothing, including the one keeping the gateway→hub rename from regressing. Fixed with a
floor on every walk. safetyCopy, responseShape and routeParity all fail correctly —
their assertions are "this is present" or they carry explicit sanity floors.
- The shape to watch: "collect offenders, assert the list is empty" passes perfectly on
  an empty scan. "assert this sentence is present" fails safely. Prefer the second, and
  where the first is necessary, assert the scan found something first.

## second copies of hub-owned vocabularies — swept
The console restates several lists the hub owns. Found three by accident (metric hints
13 names behind the drivers; capability→controls complete only by luck; rail disclosure),
so the rest are now pinned by tests: comparison operators (a closed <select>, so drift
makes an engine feature unreachable), automation trigger kinds (engine AND schema CHECK),
access-point kinds (handler AND schema CHECK), API token scopes, device kinds, wire
constants, refusal reason codes.
- The asymmetry that matters: a value the console offers and the hub rejects is VISIBLE.
  A value the hub accepts and the console never offers is INVISIBLE — no error anywhere,
  the option simply is not there.
- LESSON: a source-scanning guard must strip comments first. Commenting an option out of
  a picker left its literal in the file and the check passed on a picker that had lost
  an entry.

## §26.3 disclosure — swept, all four rails
Three of the four declared a configuration-dependent property unconditionally, and each
time the existing guard AGREED with the false claim. Now mode-aware, with a generic
endpoint test that fails if a moded rail stops passing config through.
- Telegram: webhook by default, outbound only with AQL_TELEGRAM_ENGINE=polling.
- Slack: webhook by default, outbound only when SLACK_APP_TOKEN is set.
- WhatsApp: the bridge engine changes price AND initiation (it can cold-call).
- Discord: genuinely unconditional here, but the Interactions Endpoint URL lives in
  Discord's settings and this hub cannot see it — stated in the note instead.
- LESSON: site/docs/reachability.md had Slack right all along. The prose doc was more
  accurate than the machine-readable table, which is the opposite of the usual
  assumption.

## console ↔ hub drift (tracked by src/lib/__tests__/routeParity.test.ts)
CLOSED. routeParity's KNOWN_UNAVAILABLE list is empty — every route api.ts calls, the
hub serves. Adding an entry back is a real decision, not bookkeeping.
- [ ] Phone linking is the one that was NOT a missing route: the hub has owned
      phone→member mapping since 0002 and requires verified_at to grant access, but
      nothing has ever verified a number, and both obvious shortcuts let an attacker
      squat a neighbour's number. Design + the squatting hazard: docs/PHONE-LINKING.md.
      Migration 0018 is unclaimed and reserved for it.
- Not doing: Google OAuth, and any email-based auth. The hub is meant to work off nodes;
  recovery is username-based through a delivery seam, not an email dependency.

## api & integrations
- [ ] a token-scoped `GET /v1/events` feed (today the audit log is instance-admin only)
- [ ] revoke-a-controller endpoint (no DELETE on /v1/devices yet)
- [ ] API docs with python/go/curl examples

## conformance
- [x] store/tenants.go's six superseded methods are gone; the tenancy-scoping test now
      drives the accessors production uses.
- [ ] ~32 uncalled exported symbols remain. Two of the six with NO caller at all
      (not even a test) turned out to matter: NewTelegramPoller was an entire unwired
      feature, and jcs.CanonicalizeJSON was an unrun entry point in a safety-critical
      package. The four left are UnmarshalJSON (encoding/json calls it) and three
      energy functional options. RESOLVED: all three were seams for behaviour nothing
      tested (the poller's clock on untimed readings, the per-device read timeout, the
      rollup budget), each confirmed by tampering first. They earn their place; they now
      have callers. UnmarshalJSON is called by encoding/json and is fine.
- [x] Same sweep run over TypeScript (exported VALUES with no use anywhere, counting
      in-file use — the Go version's lesson). 4 candidates from 302 exports: two false
      positives (sample code inside a CodeBlock template literal; a type-only export),
      one unused component (deleted), and one real find — src/lib/offline/grant.ts
      restates four wire constants in TS with nothing pinning them to the contract.
      Now guarded by wireConstants.test.ts against proto/vectors' spec_constants AND
      both Go declarations.
- LESSON: "no caller" is a question, not a verdict. Of the six symbols with no caller at
      all, one was an unwired feature, one an unrun entry point in a safety-critical
      package, three were untested seams, and one was a false positive. Delete last. Most are legitimate —
      test helpers, functional options, build-tagged constructors, encoding/json
      interface methods — but the sweep is worth re-running after any big change. The
      first version of it reported ZERO because a function's own doc comment counts as
      a use; strip comments before counting.

## security / ops
- [ ] grant revocation beyond "wait for the TTL" (accepted v0 non-goal today)
- [ ] OS-keychain credential vault for device/service secrets — ROADMAP Phase 2. Today
      there is no keychain/keyring code at all, and the hub's signing key + JWT secret sit
      unencrypted in the data directory
- [ ] controller position/tamper sensors return static values — wire real ones
- [ ] pen test the pairing + signed-command path against the proto vectors
- [ ] publish the aql-hub image automatically (docker.yml is workflow_dispatch only)

## hygiene
- [ ] `src-tauri`'s `system_pulse` IPC command is never called by the frontend. It is
      also cited in docs as the reason the Rust core was not chosen for the device
      engine, so deleting it costs that example — decide which.
- [ ] test the WhatsApp flow end to end at a real gate: multi-access-point picker,
      location-first flow, "hi" fallback lists linked locations or says not linked

## do not resurrect (removed by decision)
- billing / wallet / subscriptions / invoices / VAT / referrals / payouts / pricing tiers
  — Aql has NO billing system. Operators solve money outside the system; see
  ARCHITECTURE.md §4 "Money is out of scope".
- the Cloudflare Workers + Postgres backend in `backend/`. Deleted. The Go hub is the
  only server, and nothing may cite `backend/src/...` as a spec again — guarded by
  naming.test.ts.
- the SvelteKit shell, and the demo dataset it brought with it. `src/lib/demoData.ts` is
  deleted; every device row comes from the engine.
- renaming `LINTEL_*`, `lintel.db`, `_lintel._tcp` or the offline vault's IndexedDB name.
  Deployment and wire contract for hardware already in the field.
